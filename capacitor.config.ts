import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tableaudeschampions.app',
  appName: 'Tableau des Champions',
  webDir: 'client/dist'
};

export default config;
